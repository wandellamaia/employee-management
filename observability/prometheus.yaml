# my global config
global:
  scrape_interval:     1s
  evaluation_interval: 15s

# Alertmanager configuration
alerting:
  alertmanagers:
  - static_configs:
    - targets:
      # - alertmanager:9093

rule_files:

scrape_configs:
  - job_name: 'otel_exporter'
    scrape_interval: 1s
    static_configs:
      - targets: [ 'otel_collector:8889' ]
